<view class="container">
  <view class="logout">
   <button type="default" size="mini" bindtap="logout" >登出</button>
  </view>
  <view>欢迎你：{{username}}</view>
  <view class="btn">
    <button  type="primary"  bindtap="scan">扫码</button>
  </view>
  <view class="btn">
    <button type="primary" plain="true" hidden="{{hideBtnLogin}}" bindtap="sendQRCodeMsg">确认登录</button>
  </view>
  <view>{{qrMsg}}</view>
</view>